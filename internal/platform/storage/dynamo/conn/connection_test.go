package conn

/*
func TestNewDynamoDBClient(t *testing.T) {
	config := NewAWSConfig()
	client := NewDynamoDBClient(config)
	out, err := CreateChatTable(client)
	if err != nil {
		t.Error(err)
		return
	}
	log.Println(out)

	table, err := DescribeTable(client, "Chats")

	if err != nil {
		t.Error(err)
		fmt.Println(err)
	}
	log.Println(table)

}*/
